directions are dependant on whether they are using windows or Linux

Windows:
they need to view hidden folders and files and then go through to the flag

Linux:
They need to direct into the flag directory and ls -a

once the flag has been located it is encoded in base64 that is then rot13 they will need to undo those encryptions and are ultimately left with the flag with pecan{} format